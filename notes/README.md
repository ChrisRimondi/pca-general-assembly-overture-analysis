# Episode Notes

Create one markdown file per episode.

Recommended pattern:

`pca-overture-analysis-YYYY-MM-DD.md`

Start each note with a level-one heading. That heading becomes the episode title.

If a single audio file contains multiple segments, keep all segment notes in one markdown file and use segment headings inside that file. The audio file in `episodes/` must use the same slug as the note.
